<!--故意空白，防止遮挡-->
<van-cell title="  " border="{{false}}" />
<van-cell title="  " border="{{false}}" />
<van-cell title="  " border="{{false}}" />

<!--订阅领队/取消订阅-->
<form bindsubmit="SubscribeLeader" report-submit="true">
  <button form-type="submit" class="no_btn">
    <view class="fix" style="left:50rpx">
      <button style="color:#1aad19;background-color:#ffffff" size="large" plain>
        <view wx:if="{{!hasSubscribed}}">订阅领队</view>
        <view wx:else>取消订阅</view>
      </button>
    </view>
  </button>
</form>

<!--通知数量上限-->
<form bindsubmit="addCount" report-submit="true">
  <button form-type="submit" class="no_btn">
    <form bindsubmit="addCount" report-submit="true">
      <button form-type="submit" class="no_btn">
        <form bindsubmit="addCount" report-submit="true">
          <button form-type="submit" class="no_btn">
            <form bindsubmit="addCount" report-submit="true">
              <button form-type="submit" class="no_btn">
                <view class="fix" style="left:400rpx">
                  <form bindsubmit="addCount" report-submit="true">
                    <button style="color:#1aad19;background-color:#ffffff" size="large" plain>增加通知数量</button>
                  </form>
                </view>
              </button>
            </form>
          </button>
        </form>
      </button>
    </form>
  </button>
</form>

<!--文字提示-->
<van-tag type="primary" plain>订阅后该领队所发起的活动，将自动加入“我关注的”活动列表，并可设定是否接收微信通知。
</van-tag>
<van-tag type="primary" plain>当前可接收微信通知数量的上限是：{{formids.length}}，您可点击顶部按钮增加上限。
</van-tag>
<van-tag type="danger" plain>注意：由于微信限制，通知有效期为七天，过期将无法收到，需重新点击顶部按钮。</van-tag>

<!--是否接收微信服务通知-->
<van-panel wx:if="{{hasSubscribed}}" title="微信服务通知">
  <van-cell title="{{mine.acceptNotice?'接收通知':'不接收通知'}}" clickable bind:click="checkAccept" border="{{false}}">
    <van-checkbox value="{{mine.acceptNotice}}" bind:change="checkAccept" />
  </van-cell>
</van-panel>

<!--该领队发起的活动列表-->
<van-panel title="该领队所发起的活动列表">
  <van-cell wx:for="{{outdoors}}" wx:key="id" title="{{item.title}}" border="{{index<outdoors.length-1}}" />
</van-panel>